import React from "react";

const Footer = () => {
  const message = "This is a footer under construction.";

    return (
      <footer>
        <h4>{message}</h4>
      </footer>
    );
}

export default Footer;